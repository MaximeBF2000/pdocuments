import { useFormState } from '../useFormState.hook'

jest.unmock('../useFormState.hook')

describe('useFormState', () => {
  test('state contains the default provided values', () => {
    // TODO: Do the test
  })
})
